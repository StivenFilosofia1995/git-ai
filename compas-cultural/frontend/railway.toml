# =============================================
# Frontend: React/Vite (Static) — Railway config
# =============================================
# Deploy as: Docker service
# Root Directory in Railway: frontend/

[build]
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
